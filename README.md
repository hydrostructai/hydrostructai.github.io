# hydrostructai.github.io
# Tổng hợp các dự án học tập | Learning technical knowleding 📚

Chào mừng bạn đến với kho chứa các dự án học tập của tôi! Đây là nơi tôi lưu trữ và thể hiện các sản phẩm trong quá trình học tập và thực hành phát triển web. Toàn bộ trang web này được lưu trữ miễn phí bằng **GitHub Pages**.

**🚀 Xem trang web trực tiếp tại: [https://hydrostructai.github.io/](https://hydrostructai.github.io/)**

---

## ## Công nghệ sử dụng 🛠️

Các dự án trong đây chủ yếu được xây dựng bằng các công nghệ web nền tảng:
* **HTML5:** Cho cấu trúc và nội dung.
* **CSS3:** Cho việc tạo kiểu và làm đẹp giao diện.
* **JavaScript (ES6+):** Cho logic, sự tương tác và các tính năng động.

---

## ## Các dự án hiện tại ✨

### ### 1. Trực quan hóa Chuỗi Taylor cho hàm sin(x)

Đây là một công cụ tương tác được xây dựng bằng JavaScript thuần để minh họa cách một chuỗi đa thức Taylor xấp xỉ hàm `sin(x)`.

* **Mô tả:** Dự án hiển thị đồ thị của hàm `sin(x)` gốc và đồ thị của chuỗi Taylor tương ứng. Người dùng có thể thấy sự xấp xỉ trở nên chính xác hơn khi số lượng thành phần (`n`) trong chuỗi tăng lên.
* **Tính năng nổi bật:**
    * Vẽ đồ thị tương tác trên thẻ `<canvas>` của HTML5.
    * So sánh trực quan giữa hàm gốc (nét đứt) và chuỗi Taylor (nét liền).
    * Thanh trượt (slider) cho phép thay đổi số bậc (`n`) của chuỗi một cách linh hoạt.
    * Các nút điều khiển Play/Pause/Reset để xem quá trình hội tụ một cách tự động.
    * Hiển thị công thức đa thức Taylor tương ứng với bậc `n` hiện tại.

---

## ## Cách thêm một dự án mới

Để giữ cho kho chứa gọn gàng, mỗi dự án mới sẽ được đặt trong một thư mục riêng. Quy trình thực hiện như sau:

1.  Tạo một thư mục mới tại thư mục gốc cho dự án (ví dụ: `/calculator`).
2.  Đặt tất cả các tệp của dự án đó (HTML, CSS, JS) vào bên trong thư mục này.
3.  Cập nhật file `index.html` ở gốc, thêm một liên kết mới trỏ đến file HTML chính của dự án mới.
